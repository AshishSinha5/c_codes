#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n,q,x,y,*s,*s2,lastAns=0,qi,i=0;
    scanf("%d%d",&n,&q);
    s = (int[]*)(calloc(n,sizeof(int)*(10^5)));
    s2 = (int*)(malloc(n,))
    while(q>0){
        scanf("%d%d%d",&qi,&x,&y);
        switch(qi)
        {
            case 1:
        }
    }

}
